import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
    searchContainer: {
        display: 'flex',
        margin: "20px",
        width: '90vw',
    },
    grid: {
        alignItems: 'center'
    },

});